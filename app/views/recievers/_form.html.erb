<%= form_with(model: reciever) do |form| %>
  <% if reciever.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reciever.errors.count, "error") %> prohibited this reciever from being saved:</h2>

      <ul>
        <% reciever.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
